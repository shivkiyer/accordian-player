describe('readCsv', () => {
  // check if urls are playable, throw error otherwise
  // check if number of frames is integer, throw error otherwise
  // check if time instants are ok.
  // check if background color is ok.
});
